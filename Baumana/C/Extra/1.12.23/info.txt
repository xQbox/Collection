Функции с переменным числом параметров
    include <stdarg.h>

va_list vl;
va_start (vl, (имя последнего явного параметра)),

type tmp = va_arg(vl, type);
vl - тип , type - тип

va_end(vl);

---------------------Задача-------------------------------------------
    -> Написать функцию с переменным числом параметров
    - для подсчета количества строк в файлах, имена которых передаются
    - в качестве аргумента. 
