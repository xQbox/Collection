#ifndef LAB_08_LIBMY_H_
#define LAB_08_LIBMY_H_

#define MIN(a,b) (((a) < (b)) ? (a) : (b))
#define MAX(a,b) (((a) > (b)) ? (a) : (b))

#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#include "io.h"               // функции ввода/вывода
#include "alloc.h"            // динамическое выделение памяти
#include "basic.h"            // основные функции программы
#include "iotest.h"           // тесты для блока ввода вывода
#include "myerrors.h"         // декларация ошибок


#endif // LAB_08_LIBMY_H_